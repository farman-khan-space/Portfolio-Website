---
title: Home
layout: base.njk
permalink: /
---

<section class="text-center py-24 lg:py-40 px-6 bg-accent">
<div class="container mx-auto">
<h1 class="font-heading text-4xl md:text-6xl font-bold text-foreground tracking-tight">Farman Khan</h1>
<p class="font-heading mt-3 text-xl md:text-2xl font-medium text-primary">Data Analyst & Storyteller</p>
<p class="mt-6 max-w-2xl mx-auto text-lg text-foreground-muted">Transforming complex data into actionable insights and compelling narratives.</p>
<div class="mt-10">
<a href="{{ '/portfolio/' | url }}" class="inline-block bg-primary text-white font-bold py-3 px-8 rounded shadow-lg hover:scale-105 transition-transform duration-theme">
View My Work
</a>
</div>
</div>
</section>

<!-- =================================================================== -->

<!-- RECENT PROJECTS SECTION
<!-- =================================================================== -->

<section class="bg-background py-20">
<div class="container mx-auto px-6">
<div class="text-center mb-12">
<h2 class="font-heading text-3xl font-bold text-foreground">Recent Projects</h2>
<p class="mt-2 text-lg text-foreground-muted">A selection of my latest data-driven work.</p>
</div>
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">

        {% for project in collections.projects %}
          {% if loop.index0 < 3 %}
            <!-- THEME-AWARE PROJECT CARD -->
            <a href="{{ project.url | url }}" class="group block bg-accent p-2 rounded-lg shadow-md border border-transparent hover:shadow-xl hover:scale-[1.02] transition-all duration-theme flex flex-col">
                <div class="overflow-hidden rounded">
                    <img src="{{ project.data.featuredImage | url }}" alt="{{ project.data.title }}" class="w-full h-52 object-cover group-hover:scale-105 transition-transform duration-theme">
                </div>
                <div class="p-6 flex-grow">
                    <h3 class="font-heading text-xl font-bold text-foreground mb-2">{{ project.data.title }}</h3>
                    <p class="text-foreground-muted mb-4">{{ project.data.summary }}</p>
                </div>
                <div class="p-6 pt-0">
                  <span class="font-medium text-primary group-hover:underline">View Project →</span>
                </div>
            </a>
          {% endif %}
        {% endfor %}
        
    </div>
    <div class="text-center mt-16">
        <!-- SECONDARY BUTTON STYLE -->
        <a href="{{ '/portfolio/' | url }}" class="inline-block bg-accent text-primary font-medium py-3 px-8 rounded border border-primary hover:bg-primary hover:text-white transition-colors duration-theme">
           View All Projects
        </a>
    </div>
</div>

</section>

<!-- =================================================================== -->

<!-- LATEST BLOG POSTS SECTION
<!-- =================================================================== -->

<section class="bg-background py-20">
<div class="container mx-auto px-6">
<div class="text-center mb-12">
<h2 class="font-heading text-3xl font-bold text-foreground">Latest from the Blog</h2>
<p class="mt-2 text-lg text-foreground-muted">My recent thoughts on data, analytics, and storytelling.</p>
</div>
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">

        {% for post in collections.posts %}
          {% if loop.index0 < 3 %}
            <!-- THEME-AWARE BLOG POST CARD -->
            <a href="{{ post.url | url }}" class="group block bg-background p-8 rounded-lg shadow-md border border-transparent hover:shadow-xl hover:scale-[1.02] transition-all duration-theme flex flex-col">
                <div class="flex-grow">
                    <p class="text-sm text-foreground-muted mb-2">{{ post.data.date }} • {{ post.data.readTime }}</p>
                    <h3 class="font-heading text-xl font-bold text-foreground mb-3">{{ post.data.title }}</h3>
                    <p class="text-foreground-muted mb-6">{{ post.data.summary }}</p>
                </div>
                <span class="font-medium text-primary group-hover:underline">Read Article →</span>
            </a>
          {% endif %}
        {% endfor %}

    </div>
    <div class="text-center mt-16">
        <!-- SECONDARY BUTTON STYLE -->
        <a href="{{ '/blog/' | url }}" class="inline-block bg-accent text-primary font-medium py-3 px-8 rounded border border-primary hover:bg-primary hover:text-white transition-colors duration-theme">
           View All Posts
        </a>
    </div>
</div>

</section>